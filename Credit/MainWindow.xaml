<Window x:Class="Credit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Credit"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="textBox" Grid.Row="2" Margin="10" Text="Enter your monthly income"/>
        <Slider x:Name="slider" Grid.Row="1" Margin="10" Minimum="0" Maximum="100000" TickFrequency="1" IsSnapToTickEnabled="True"/>
        <TextBlock Name="credit" Grid.Row="0" Margin="10" Text="Allowable loan amount:"/>
    </Grid>
</Window>
